<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MSC.CM.XaSh.Views.AnnouncementsPage"
             xmlns:vm="clr-namespace:MSC.CM.XaSh.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:AnnouncementsViewModel />
    </ContentPage.BindingContext>


    <ContentPage.Resources>

    </ContentPage.Resources>

    <ContentPage.Content>
        <ListView Margin="20" ItemsSource="{Binding Announcements}" ItemSelected="OnItemSelected">
            <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Margin="10">
                        <Label Text="{Binding Title}" FontAttributes="Bold" LineBreakMode="WordWrap" />
                        <Label Text="{Binding Description}" FontAttributes="Italic" LineBreakMode="WordWrap" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>

</ContentPage>
